name: Project CI

on:
  push:
    branches: [main, staging]
  pull_request:
    branches: [main, staging]

jobs:
  # --- Backend CI ---
  backend:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    services:
      db:
        image: postgres:15-alpine
        env:
          POSTGRES_PASSWORD: password
          POSTGRES_DB: db
      redis:
        image: redis:7-alpine
    steps:
      - uses: actions/checkout@v4

      - name: Lowercase repo name
        run: echo "repo_name=$(echo ${{ github.repository }} | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and Load Backend
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          load: true
          tags: ghcr.io/${{ env.repo_name }}/backend:test
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Run Backend Tests
        run: |
          docker run --rm --network host -e PYTHONPATH=/app -e NUMBA_CACHE_DIR=/tmp ghcr.io/${{ env.repo_name }}/backend:test python -m pytest

      - name: Push Backend Image
        if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/staging')
        run: |
          TAG_NAME=${{ github.ref == 'refs/heads/main' && 'latest' || 'staging' }}
          docker tag ghcr.io/${{ env.repo_name }}/backend:test ghcr.io/${{ env.repo_name }}/backend:$TAG_NAME
          docker push ghcr.io/${{ env.repo_name }}/backend:$TAG_NAME

  # --- Frontend CI ---
  frontend:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Lowercase repo name
        run: echo "repo_name=$(echo ${{ github.repository }} | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and Load Frontend
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          load: true
          tags: ghcr.io/${{ env.repo_name }}/frontend:test
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Run Frontend Lint and Test
        run: |
          docker run --rm ghcr.io/${{ env.repo_name }}/frontend:test npm run lint
          docker run --rm ghcr.io/${{ env.repo_name }}/frontend:test npm test

      - name: Push Frontend Image
        if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/staging')
        run: |
          TAG_NAME=${{ github.ref == 'refs/heads/main' && 'latest' || 'staging' }}
          docker tag ghcr.io/${{ env.repo_name }}/frontend:test ghcr.io/${{ env.repo_name }}/frontend:$TAG_NAME
          docker push ghcr.io/${{ env.repo_name }}/frontend:$TAG_NAME
